# ============================================
# Minimarket Camucha - Variables de Entorno
# ============================================
# Copia este archivo como .env y configura los valores para producción
# 
# NOTA: El sistema ya tiene configuraciones por defecto para:
# - Email SMTP (Gmail): minimarket.camucha@gmail.com
# - Google OAuth: Ya configurado
# - Logging: Serilog a consola y archivos en logs/minimarket-.txt
# - Resend API: Se usa automáticamente como fallback si SMTP falla
#
# Solo necesitas configurar las variables marcadas como OBLIGATORIAS

# ============================================
# BASE DE DATOS (OBLIGATORIO)
# ============================================
# Connection string para SQL Server
# Ejemplo: Server=tu-servidor.database.windows.net,1433;Database=MinimarketDB;User Id=usuario;Password=contraseña;Encrypt=True;TrustServerCertificate=False;
DB_CONNECTION_STRING=

# ============================================
# JWT AUTHENTICATION (OBLIGATORIO)
# ============================================
# Clave secreta para JWT (MÍNIMO 32 caracteres, recomendado 64+)
# Genera una clave segura: openssl rand -base64 64
JWT_SECRET_KEY=
JWT_ISSUER=MinimarketAPI
JWT_AUDIENCE=MinimarketWeb

# ============================================
# EMAIL SETTINGS (OPCIONAL - Ya configurado por defecto)
# ============================================
# El sistema usa SMTP de Gmail por defecto, y Resend API como fallback automático
# Solo configura estas variables si quieres cambiar los valores por defecto
#
# Opción 1: SMTP (Gmail - configurado por defecto)
# Valores por defecto:
#   SMTP_SERVER=smtp.gmail.com
#   SMTP_USER=minimarket.camucha@gmail.com
#   SMTP_PASSWORD=xzloatedigfqgyxi (configurar solo si cambias la contraseña)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=minimarket.camucha@gmail.com
SMTP_PASSWORD=xzloatedigfqgyxi
FROM_EMAIL=minimarket.camucha@gmail.com
FROM_NAME=Minimarket Camucha

# Opción 2: Resend API (fallback automático si SMTP falla)
# Solo necesario si quieres usar Resend como método principal
# Si no se configura, el sistema usará SMTP y solo usará Resend si SMTP falla
RESEND_API_KEY=

# ============================================
# URLs Y CORS (OBLIGATORIO)
# ============================================
# URL base de la API (sin /api al final)
# Ejemplo: https://api.minimarketcamucha.com
BASE_URL=

# URL del frontend
# Ejemplo: https://minimarketcamucha.com
FRONTEND_URL=

# Orígenes permitidos para CORS (separados por coma)
# Ejemplo: https://minimarketcamucha.com,https://www.minimarketcamucha.com
CORS_ORIGINS=

# ============================================
# GOOGLE OAUTH (OPCIONAL - Ya configurado por defecto)
# ============================================
# El sistema ya tiene Google OAuth configurado con estos valores:
#   ClientId: 259590059487-5k8bk2sor6r9oa02pojkhj5nrd8c9h2e.apps.googleusercontent.com
#   ClientSecret: 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
# Solo actualiza si necesitas usar credenciales diferentes
GOOGLE_CLIENT_ID=259590059487-5k8bk2sor6r9oa02pojkhj5nrd8c9h2e.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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
GOOGLE_REDIRECT_URI=

# ============================================
# LOGGING (Ya configurado - No requiere configuración)
# ============================================
# El sistema usa Serilog configurado para:
# - Consola: Todos los logs
# - Archivos: logs/minimarket-YYYYMMDD.txt (retiene 30 días)
# - Niveles: Information por defecto, Warning para Microsoft/System
# No requiere configuración adicional

# ============================================
# DOCKER (Opcional - para docker-compose)
# ============================================
# Puerto para la API (default: 8080)
PORT=8080

# Puerto para el frontend (default: 80)
WEB_PORT=80

